using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GridTorch : MonoBehaviour
{

    public int i,j;
    public float torch;


    // Start is called before the first frame update
    void Start()
    {
        torch = 0.0f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
